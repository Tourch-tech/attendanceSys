<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <style>
        *{
            margin: 0;
            padding: 0;
            font-family: sans-serif;
        }
        body{
            overflow-x: hidden;
            background-color: #d9d9d9;
        }
        .head{
            background-color: #a61b26;
            color: white;
            width: 100%;
            overflow: hidden;
            height: 40px;
            display: flex;
            justify-content: space-between;
            padding: 12px 27px 12px;
            margin-bottom: 70px;
        }
        .nav-link{
            text-decoration: none;
            color: #ffffff;
            font-size: 13px;
            margin-right: 45px;
        }
        .nav-link:hover{
            font-size: 14px;
            transition: .4s;

        }
        .name{
            color: #0d0d0d;
            height: 40px;
            text-transform: uppercase;
            font-size: 28px;
            margin: 12px 0 0;
        }
        .date{
            
            border-bottom: 1px solid #0d0d0d;
            font-size: 12px;
            width: 100%;
            padding: 8px;
        }
        .card{
            width: 100%;
            background-color: #d9d9d9;
            border-radius: 8px;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            box-shadow: 10px 5px 5px #c3c3c3;
            padding: 12px;
        }
        h1{
            padding-left: 40px;
        }
        .phone{
            font-size: 8px;
        }
    </style>
</head>
<body>
    <div class="head">
        <h1>Lincoln Library Attendance</h1>
        <a class="nav-link" href="/signAttendance">ADD ATTENDANCE</a>  
    </div>
    <div class="card">    
        <% for (let i = data.length-1; i >= 0; i--) { %>
            <div>
                <h1 class="name">
                <%= data[i].name %>
                </h1>
                <p class="phone">
                <%= data[i].phone %>
                </p>
                <p class="date">
                <%= data[i].date %>
                </p>
            </div>
            
        <% } %>
    </div>
</body>
</html>